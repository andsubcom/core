import { ethers } from "hardhat"

async function main() {
  // TODO: deploy ERC20 token
}

main()
      .then(() => process.exit(0))
      .catch(error => {
          console.log(error)
          process.exit(1)
      })
